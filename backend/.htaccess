# Allow API endpoints; block credential files
<FilesMatch "^(config|config\.local|auth|helpers)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
